; This file is INCLUDEd twice:
; - for HMMoves in home/names.asm
; - for HMMoveArray in engine/pokemon/bills_pc.asm

	;db -1 ; end 
	;FIXED: no restrictions around HM moves (can be deleted, can be put into the daycare with HM moves, etc.)
